services:
  redis:
    image: redis:7-alpine
    ports:
      - "8101:6379"

  app:
    build: ./app
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - "8102:5000"
    depends_on:
      - redis
